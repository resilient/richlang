<!DOCTYPE html>
<html>
<head>
  <title>Rich Lang</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="container">

  <div class="masthead">
    <div class="navbar">
      <div class="navbar-inner">
        <div class="container">
          <%= link_to 'Rich Lang', root_path, class: "brand" %>
	        <% if user_signed_in? %>
            <p class="navbar-text pull-right">
              Привіт, <%= link_to current_user.name, edit_user_registration_path, class: "navbar-link" %> |
              <%= link_to 'Вийти', destroy_user_session_path, method: :delete, class: "navbar-link" %>
            </p>

            <ul class="nav">
              <li><%= link_to 'Словник', words_path %></li>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Тренування <b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <li><a href="#">Слово -> Переклад</a></li>
                  <li><a href="#">Переклад -> Слово</a></li>
                </ul>
              </li>
            </ul>

        <% else %>
            <p class="navbar-text pull-right">
              <%= link_to 'Увійдіть', new_user_session_path, class: "navbar-link" %> або
              <%= link_to 'Зареєтруйтеся', new_user_registration_path, class: "navbar-link" %>
            </p>
	        <% end %>
        </div>
      </div>
    </div>
  </div>

  <% flash.each do |name, msg| %>
    <%= content_tag :div, msg, class: "alert alert-#{name}" %>
  <% end %>

	<%= yield %>

  <hr>

  <div class="footer">
    <p>© Rich Lang, 2013</p>
  </div>

  <br/>
  <br/>
  <br/>
  <br/>
  <br/>
  <br/>
  <br/>
  <%= debug(params) if Rails.env.development? %>
  <%#= debug(session) if Rails.env.development? %>
</div>

</body>
</html>
